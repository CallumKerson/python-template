[project]
dependencies = []
description = "Add your description here"
name = "{{ project_name }}"
readme = "README.md"
requires-python = ">=3.12"
version = "0.1.0"

[dependency-groups]
dev = ["ruff>=0.12.10"]

[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]

[tool.hatch.build.targets.wheel]
packages = ["{% include 'package-name.jinja' %}"]
